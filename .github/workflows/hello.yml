name: Hello Workflow

on: push

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print Message
        run: echo "Hello, World!"
      - name: Show Node Version
        run: node -v
      - name: Show Current Folder
        run: pwd
  